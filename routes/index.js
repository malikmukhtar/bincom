var express = require('express');
var router = express.Router();
const connection = require('../models/db'); // database module

/* GET home page. */
// router.get('/', (req, res) => {
  
//   res.send('My lovely BinCom1');
// });

// module.exports = router;
